
(function() {
  "use strict";

  /* Testimonials Slider */
  var options = {
    type: 'carousel',
    perView: 1.3,
    gap: 48
  }
  
  var glide = new Glide('.testimonials__slider', options).mount()
  
})()